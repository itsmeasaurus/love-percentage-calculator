# Love Percentage Calculator

A playful web app that calculates a compatibility score between two names. Enter names, get a love percentage, and share the result as an image.

## Run

Open `index.html` in a browser, or serve the folder with any static server.

## Tech

- HTML, CSS, JavaScript (no build step)
- Tailwind CSS
- html2canvas for image export
